<beans xmlns="http://www.springframework.org/schema/beans"
xmlns:s="http://www.springframework.org/schema/security"
xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
xsi:schemaLocation="http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
http://www.springframework.org/schema/security
http://www.springframework.org/schema/security/spring-security-3.0.xsd">

	
	<s:http auto-config='true' use-expressions="true">
		<s:intercept-url pattern="/fletes" access="isAnonymous()"/>
		<s:intercept-url pattern="/Login" access="isAnonymous()"/>
		<s:form-login login-page="/Login" authentication-failure-url="/Login?login_error=true" />
		<s:intercept-url pattern="/static/**" access="isAuthenticated()" />
		<s:intercept-url pattern="/**" access="isAuthenticated()" />
		<s:intercept-url pattern="/index" access="isAuthenticated()" />
		<s:intercept-url pattern="/Constants" access="isAuthenticated()" />
	</s:http>
	<s:authentication-manager >
		<s:authentication-provider user-service-ref="userService"/>
	</s:authentication-manager>
</beans>